#include<bits/stdc++.h>
using namespace std;

int all_index(int* arr, int size, int x, vector<int> &ans){
	if(size == 0) return 0;
	if
}

int main(){
	int arr[] = {5,6,5,5,6,5};
	int size = 6;
	int x = 5;
	vector<int> ans;
	cout << all_index(arr, size, x, ans);
}
